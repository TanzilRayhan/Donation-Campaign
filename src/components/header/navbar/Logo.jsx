import companyLogo from '../../../assets/Logo.png'

const Logo = () => {
    return (
        <div>
            <div>
            <img src={companyLogo} alt="Donation Campaign Logo"/>  
            </div>
        </div>
    );
};

export default Logo;